from django.urls import include, path

from api.views import (
    AdminActivityAPIView,
    AdminAnalyticsAPIView,
    AdminProctoringAPIView,
    AdminReportsAPIView,
    AdminResultsAPIView,
    AdminSecurityAlertsAPIView,
    AdminSummaryAPIView,
    AdminUsersAPIView,
    AdminTeachersAPIView,
    SystemSettingsAPIView,
    StudentDashboardAPIView,
    StudentExamsAPIView,
    StudentNotificationsAPIView,
    StudentResultsOverviewAPIView,
    TeacherAnalyticsAPIView,
    TeacherExamsAPIView,
    TeacherStudentsAPIView,
    TeacherReportsAPIView,
    TeacherResultsAPIView,
    TeacherMonitorAPIView,
    TeacherSummaryAPIView,
    SaveExamProgressAPIView,
)

urlpatterns = [
    path('', include('users.urls')),
    path('', include('exams.urls')),
    path('', include('results.urls')),
    path('', include('proctoring.urls')),
    path('dashboard/student/', StudentDashboardAPIView.as_view(), name='dashboard-student'),
    path('student/exams/', StudentExamsAPIView.as_view(), name='student-exams'),
    path('student/results/overview/', StudentResultsOverviewAPIView.as_view(), name='student-results-overview'),
    path('notifications/', StudentNotificationsAPIView.as_view(), name='notifications-student'),
    path('admin/summary/', AdminSummaryAPIView.as_view(), name='admin-summary'),
    path('admin/analytics/', AdminAnalyticsAPIView.as_view(), name='admin-analytics'),
    path('admin/security-alerts/', AdminSecurityAlertsAPIView.as_view(), name='admin-security-alerts'),
    path('admin/activity/', AdminActivityAPIView.as_view(), name='admin-activity'),
    path('admin/users/', AdminUsersAPIView.as_view(), name='admin-users'),
    path('admin/teachers/', AdminTeachersAPIView.as_view(), name='admin-teachers'),
    path('admin/results/', AdminResultsAPIView.as_view(), name='admin-results'),
    path('admin/proctoring/', AdminProctoringAPIView.as_view(), name='admin-proctoring'),
    path('admin/reports/', AdminReportsAPIView.as_view(), name='admin-reports'),
    path('admin/settings/', SystemSettingsAPIView.as_view(), name='admin-settings'),
    path('teacher/summary/', TeacherSummaryAPIView.as_view(), name='teacher-summary'),
    path('teacher/monitor/', TeacherMonitorAPIView.as_view(), name='teacher-monitor'),
    path('teacher/analytics/', TeacherAnalyticsAPIView.as_view(), name='teacher-analytics'),
    path('teacher/exams/', TeacherExamsAPIView.as_view(), name='teacher-exams'),
    path('teacher/students/', TeacherStudentsAPIView.as_view(), name='teacher-students'),
    path('teacher/results/', TeacherResultsAPIView.as_view(), name='teacher-results'),
    path('teacher/reports/', TeacherReportsAPIView.as_view(), name='teacher-reports'),
    path('exam/save/', SaveExamProgressAPIView.as_view(), name='exam-save'),
    path('exam/progress/', SaveExamProgressAPIView.as_view(), name='exam-progress'),
]
